# Decorator-Practice
Python decorator practice project demonstrating how decorators wrap functions, add behavior before and after execution, and dynamically access function names using __name__. Includes reusable decorators applied to multiple functions.
